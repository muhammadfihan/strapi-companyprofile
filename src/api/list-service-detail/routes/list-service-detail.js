'use strict';

/**
 * list-service-detail router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::list-service-detail.list-service-detail');
